import 'package:flutter/material.dart';

class AltroTab extends StatelessWidget {
  const AltroTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Non ci sono prodotti in questa sezione'),
    );
  }
}
